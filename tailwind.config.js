const colors = require('tailwindcss/colors');
module.exports = {
  mode: 'jit',
  purge: [],
  darkMode: false, // or 'media' or 'class'
  theme: {
    fontFamily: {
      'oswald': ['Oswald'],
    },
    color: {
    },
  },
  variants: {
    extend: {},
  },
  plugins: [],
}
